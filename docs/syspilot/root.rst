Graph Root
==========

The single entry point of the syspilot v2 traceability graph. Every User Story
self-declares membership via ``:tracked_by:`` — impact analysis and orphan
detection start here, deterministically, instead of relying on a "type == story"
convention.

.. root:: syspilot
   :id: ROOT_SYSPILOT
   :status: approved

   Anchor node for the syspilot v2 specification graph.
